pipeline {
    agent any
    
    stages {

        //
        stage("mvnw package") {
            steps {
                sh "amvn clean package"
            }
        }
        
        //
        
        stage("java -jar target/*.jar") {
            steps {
                sh "java -jar target/*.jar"
            }
        }
    }
}
